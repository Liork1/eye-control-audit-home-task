# Supabase project URL (base URL only — no /rest/v1/ suffix)
SUPABASE_URL=https://your-project.supabase.co

# Supabase service role key (server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase anon/public key (optional; falls back to service role for RLS tests)
SUPABASE_ANON_KEY=your-anon-key

# Must match Supabase Project Settings → API → JWT Secret
JWT_SECRET=your-jwt-secret

# Direct Postgres connection (required for npm run db:migrate)
# Supabase Dashboard → Project Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
